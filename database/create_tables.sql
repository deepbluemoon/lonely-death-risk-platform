-- =========================================================
-- 1. 고독사 지역통합
-- 기존 테이블이 이미 있으므로 생성하지 않음
-- =========================================================

-- 확인용
DESC TBL_LONELY_DEATH;


-- =========================================================
-- 2. 미충족 의료율
-- 기존 테이블이 이미 있으므로 생성하지 않음
-- =========================================================

DESC TBL_UNMET_MEDICAL;


-- =========================================================
-- 3. 사회복지 종사자 데이터
-- CSV 전체 연도 데이터를 저장
-- =========================================================

CREATE TABLE TBL_WELFARE_WORKERS (
    REGION VARCHAR2(50) NOT NULL,
    YEAR NUMBER(4) NOT NULL,
    SOCIAL_WORKERS NUMBER,
    POPULATION NUMBER,
    PEOPLE_PER_WORKER NUMBER(10,2),

    CONSTRAINT PK_TBL_WELFARE_WORKERS
        PRIMARY KEY (REGION, YEAR)
);


-- =========================================================
-- 4. 데이터 확인
-- =========================================================

DESC TBL_WELFARE_WORKERS;


-- =========================================================
-- 5. 인덱스
-- 지역 + 연도 조회 최적화
-- =========================================================

CREATE INDEX IDX_WELFARE_REGION_YEAR
ON TBL_WELFARE_WORKERS (REGION, YEAR);


COMMIT;